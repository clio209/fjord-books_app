<%= form_with(model: current_user.active_favorites.find_by(followed_id: @user.id),
              html: { method: :delete }, local: true) do |f| %>
  <%= f.submit t('views.common.unfollow'), class: "btn" %>
<% end %>
